apiVersion: v1
kind: Service
metadata:
  name: ggdeeq
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ggdeeq"
    port: 8080
  selector:
    app: ggdeeq
